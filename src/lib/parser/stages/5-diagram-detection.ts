import { ParserState } from "@/lib/parser/types";

export async function detectDiagrams(state: ParserState): Promise<ParserState> {
  // Diagram detection and understanding are now merged into Stage 6 using Gemini.
  return state;
}
